dist
coverage
node_modules
pnpm-lock.yaml
package-lock.json
src/api/generated.ts

# 上游语言包，**逐字节复制**自 frontend/src/i18n/lang/（F13）。
# 不能格式化：`src/i18n/lang-parity.test.ts` 断言的是与上游**逐字节相同**，
# prettier 一改（缩进/换行/尾逗号）那道守卫立刻红，而它正是用来防止
# 我们和上游的文案契约悄悄分叉的。要更新就重新 cp 一遍上游的。
src/i18n/lang/
